name: Refresh Strategy Radar

on:
  workflow_dispatch:
  schedule:
    - cron: '15 5 * * *'
    - cron: '0 6 * * 3'
  push:
    branches: [main]
    paths:
      - '.strategy-radar-refresh'
      - 'config/**'
      - 'scripts/**'
      - 'docs/**'
      - 'site/**'
      - 'tests/**'
      - '.github/workflows/pages.yml'
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: strategy-radar-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install feedparser PyYAML beautifulsoup4 pytest
      - run: python -m pytest tests/test_static_pipeline.py tests/test_intelligence.py -q
      - run: node --check docs/radar.js
      - run: node --test tests/static_ui.test.cjs
      - run: python scripts/validate_static.py
  refresh:
    if: github.event_name != 'pull_request'
    needs: validate
    permissions:
      contents: write
      pages: read
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install collector dependencies
        run: pip install feedparser PyYAML beautifulsoup4

      - name: Collect market intelligence
        run: |
          python scripts/archive_news.py
          python scripts/build_static_news.py

      - name: Add official, research and global sources
        run: python scripts/augment_strategy_radar.py

      - name: Build verified reports library
        run: python scripts/build_reports.py

      - name: Build key market metrics
        run: python scripts/build_metrics.py

      - name: Collect verified official brand accounts
        env:
          VK_ACCESS_TOKEN: ${{ secrets.VK_ACCESS_TOKEN }}
        run: python scripts/collect_social.py

      - name: Verify articles and build saved editorial editions
        run: |
          python scripts/enrich_sources.py
          python scripts/intelligence.py

      - name: Build last-known-good browser snapshot
        run: python scripts/build_fallback_bundle.py

      - name: Validate publication
        run: python scripts/validate_static.py --fresh

      - name: Stage generated snapshot
        run: |
          mkdir -p /tmp/strategy-radar/docs-reports
          cp data/news.json /tmp/strategy-radar/news.json
          cp data/reports.json /tmp/strategy-radar/reports.json
          cp data/metrics.json /tmp/strategy-radar/metrics.json
          cp data/{archive,social,enrichment,coverage,editions,intelligence}.json /tmp/strategy-radar/
          git rev-parse HEAD > /tmp/strategy-radar/source-head
          cp docs/fallback-data.js /tmp/strategy-radar/fallback-data.js
          if [ -d docs/reports ]; then
            cp -a docs/reports/. /tmp/strategy-radar/docs-reports/
          fi

      - name: Save refreshed archive safely
        run: |
          set -e
          git config user.name "strategy-radar-bot"
          git config user.email "actions@users.noreply.github.com"

          restore_snapshot() {
            mkdir -p data docs/data docs/reports
            cp /tmp/strategy-radar/news.json data/news.json
            cp /tmp/strategy-radar/news.json docs/data/news.json
            cp /tmp/strategy-radar/reports.json data/reports.json
            cp /tmp/strategy-radar/reports.json docs/data/reports.json
            cp /tmp/strategy-radar/metrics.json data/metrics.json
            cp /tmp/strategy-radar/metrics.json docs/data/metrics.json
            cp /tmp/strategy-radar/{archive,social,enrichment,coverage,editions,intelligence}.json data/
            cp /tmp/strategy-radar/intelligence.json docs/data/intelligence.json
            cp /tmp/strategy-radar/fallback-data.js docs/fallback-data.js
            if [ -d /tmp/strategy-radar/docs-reports ]; then
              cp -a /tmp/strategy-radar/docs-reports/. docs/reports/
            fi
          }

          for attempt in 1 2 3; do
            git fetch origin main
            if ! git diff --quiet "$(cat /tmp/strategy-radar/source-head)" origin/main -- config scripts docs/index.html docs/radar.js .github/workflows/pages.yml; then
              echo "Collector or editorial configuration changed during collection. The newer run will publish its own snapshot."
              exit 1
            fi
            git reset --hard origin/main
            restore_snapshot
            git add data/news.json docs/data/news.json                     data/reports.json docs/data/reports.json                     data/metrics.json docs/data/metrics.json                     docs/fallback-data.js docs/reports
            git add data/archive.json data/social.json data/enrichment.json data/coverage.json data/editions.json data/intelligence.json docs/data/intelligence.json

            if git diff --cached --quiet; then
              echo "No data changes"
              exit 0
            fi

            git commit -m "data: refresh Strategy Radar"

            if git push origin HEAD:main; then
              echo "Refresh pushed successfully"
              exit 0
            fi

            echo "Push race detected, retrying ($attempt/3)"
            sleep $((attempt * 3))
          done

          echo "Unable to push refreshed snapshot after 3 attempts"
          exit 1

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload website
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    if: github.event_name != 'pull_request'
    needs: refresh
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
